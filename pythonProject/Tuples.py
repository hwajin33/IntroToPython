
coordinates = (4, 5)

# accessing the elements inside the tuple
# but can't change the value (immutable) -> coordinates[1] = 10 => differences between lists
print(coordinates[1])

# list of tuples
coordinates = [(4, 5), (3, 6), (7, 9)]
